name: tvm-builder
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: .github/workflows/build.yml

